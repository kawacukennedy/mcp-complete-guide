apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-python-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-python-server
  template:
    metadata:
      labels:
        app: mcp-python-server
    spec:
      containers:
      - name: server
        image: yourdocker/repo:mcp-python
        ports:
        - containerPort: 8080
        - containerPort: 9090
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-python-server
spec:
  selector:
    app: mcp-python-server
  ports:
  - port: 8080
    targetPort: 8080
  - port: 9090
    targetPort: 9090
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-python-server-netpol
spec:
  podSelector:
    matchLabels:
      app: mcp-python-server
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from: []
    ports:
    - protocol: TCP
      port: 8080
  egress: []